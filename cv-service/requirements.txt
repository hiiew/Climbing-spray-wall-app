# cv-service/requirements.txt
# Pin to specific minor versions for reproducibility.
# Separate optional GPU section at bottom — uncomment for CUDA deployments.

# ── Web framework ────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ── Data validation ──────────────────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1

# ── HTTP client (async, for image downloads & callbacks) ─────────────────────
httpx==0.27.0

# ── Computer vision ──────────────────────────────────────────────────────────
opencv-python-headless==4.9.0.80   # headless = no GUI deps (server-safe)
Pillow==10.3.0
numpy==1.26.4

# ── Object detection — YOLO v8 ───────────────────────────────────────────────
ultralytics==8.2.18                # Includes YOLO v8 model + inference utilities

# ── Segment Anything Model (SAM) — for accurate mode ─────────────────────────
# segment-anything @ git+https://github.com/facebookresearch/segment-anything.git
# Uncomment when enabling ACCURATE detection mode.

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0

# ── Observability ─────────────────────────────────────────────────────────────
prometheus-fastapi-instrumentator==6.1.0

# ── Optional: GPU (uncomment for CUDA 12.x environments) ─────────────────────
# torch==2.3.0+cu121    --index-url https://download.pytorch.org/whl/cu121
# torchvision==0.18.0+cu121
